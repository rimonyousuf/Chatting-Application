import { Box } from "@mui/material";

const ChatArea = () => {
    return (
        <Box sx={{
            overflow: "auto",
            flex: "1 0 0",
            background: "#eee"
        }}>
            <span>Chatarea</span>
        </Box>
    );
};

export default ChatArea;